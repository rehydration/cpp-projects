/*

 */

#include <iostream>
#include <cstring>
#include <cctype>


struct Node {
  char data;
  Node* next = NULL;
};

void push(Node*& head, char t) {
  head
}

Node* pop(Node* head) {

}

Node* peek(Node* head) {

}


int main() {
  Node* stack_head;
  Node* 
  
  
  std::cout << "Enter an expression:\n";
  while (true) {
    char token;
    std::cin >> token;
    if (std::isdigit(token)) {
    }
    else {
      push(stack_head, token);
    }

  }




  return 0;
}
